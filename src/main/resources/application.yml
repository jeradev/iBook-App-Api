## PostgreSQL
server.port: 8090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/iBook
    username: postgres
    password: passw0rd
  jpa:
    hibernate.ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

#drop n create table again, good for testing, comment this in production
